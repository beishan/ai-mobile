自动分页第一页开始。这个文本没有内置分页符，阅读器需要根据页面容量自动拆页。第一段保留足够长度，让分页器必须继续向后寻找安全边界。
第二段继续扩展内容。黑白墨水屏刷新慢，但分页索引应该在进入阅读之前准备好，翻页时只读取当前页缓存。
第三段用于验证中文 UTF-8 字符不会被截断。每一页都应该是完整字符串，不能把一个汉字拆成无效字节。
第四段补足文本长度。后续接入 SD 卡 TXT 文件时，也会走同一条分页路径。
