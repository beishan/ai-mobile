自动分页第一页开始。这个文本没有内置分页符，阅读器需要根据页面容量自动拆页。第一段保留足够长度，让分页器必须继续向后寻找安全边界。黑白墨水屏刷新慢，但分页索引应该在进入阅读之前准备好，翻页时只读取当前页缓存。这段文字需要足够长，才能在更大的页面容量下仍然产生多页分割。每一页都应该是完整字符串，不能把一个汉字拆成无效字节。后续接入 SD 卡 TXT 文件时，也会走同一条分页路径。这里继续补充内容，确保总长度超过单页容量，触发自动分页逻辑。中文 UTF-8 编码下每个汉字占三个字节，分页器需要在字节边界上做安全裁剪。这段文字的目的是验证分页算法在较长文本上的正确性。分页器应该能在段落或词语边界处断开，而不是在汉字中间截断。最后再来一段，确保文本足够长，能够产生至少两到三页的内容。阅读器在加载文本时会先扫描全文，计算总页数，然后按需生成每页的缓存。翻页时直接从缓存读取，不需要重新分页。这样的设计让翻页速度很快，适合墨水屏的刷新特性。
